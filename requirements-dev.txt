# Development requirements for Qustodio Home Assistant integration
# Requires Python 3.13+

# Home Assistant core
homeassistant>=2024.1.0

# Core dependencies with compatibility fixes
aiohttp>=3.8.0
voluptuous
async-timeout

# Compatibility fixes for josepy/acme conflict
josepy<1.14.0
acme>=1.21.0

# pycares 5.x removed `ares_query_a_result`, which the aiodns 3.x version
# pinned by Home Assistant references in its type annotations. On Python
# <=3.13 those annotations are evaluated eagerly at import, raising
# AttributeError. Constrain pycares to the 4.x line aiodns 3.x supports.
pycares>=4.9.0,<5

# Development tools
black
flake8
mypy
pylint
isort
yamllint
codespell
pre-commit

# Testing framework
pytest>=6.2.5
pytest-asyncio
pytest-cov
pytest-xdist
pytest-mock

# Additional testing utilities
freezegun  # For date/time mocking
responses  # For HTTP mocking

# Missing Home Assistant dependencies
numpy  # Required by stream component
scipy  # Often required alongside numpy
